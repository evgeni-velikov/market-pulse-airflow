name: crypto_pulse
version: "1.0.0"
profile: crypto-pulse

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
analysis-paths: ["analyses"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  crypto_pulse:
    bronze:
      +materialized: view
      +schema: bronze
    silver:
      +materialized: incremental
      +schema: silver
    gold:
      dimensions:
        +materialized: table
        +schema: gold
      marts:
        +materialized: incremental
        +schema: gold
